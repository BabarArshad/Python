# PYTHON FUNCTIONS 

# We use functions to group code into reusable blocks. Functions allow us to break our code into smaller, more manageable pieces.

def my_function():
    print("Hello from Babar!")
    x = 10 
    print (x ** x)

# at this stage if we print the function, it will not execute the code inside it. It will only print the function object.
# lets call the function to execute the code inside it.
my_function()

# we can print as many times as we want.
my_function()
my_function()
my_function()







